"use strict";// treat all JS code as newer version

// alert(3+3)// we are using nodejs, not browser, there different syntax in nodejs
// Premetive DataTypes:
let name="Mayur"// string
let age=21// number
let isTrue=true// boolean
let temperature=null// null => standalone value. empty.
let car;// undefined=> value yet not assigned
// symbol => unique

// object
console.log(typeof null); // object
console.log(typeof undefined); // undefined


